# Minimal requirements for running notebooks on Google Colab.
# Before installing, clone the repo in Colab and cd into it:
#
# Google Colab pre-installs: numpy, pandas, matplotlib, scikit-learn, tensorflow, scipy, pillow
# On installe uniquement les packages manquants ou spécifiques:

seaborn>=0.13.2
tqdm>=4.67.1

# Note: Si erreur numpy binary compatibility, ajouter:
# numpy==1.26.4  # Décommenter uniquement si erreur "numpy.dtype size changed"









