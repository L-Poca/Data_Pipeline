# Minimal requirements for running src/notebooks/Main_Revamp.ipynb on Google Colab.
# Before installing, clone the repo in Colab and cd into it:
#   !git clone https://github.com/L-Poca/Data_Pipeline.git
#   %cd Data_Pipeline
# Then install:
#   !pip install -r requirements_colab.txt
#
# These packages are necessary and sufficient for the imports and code used
# in the notebook (data loading + simple ML pipeline + plotting).
numpy>=1.23
Pillow>=9.0
scikit-learn>=1.0
matplotlib>=3.5
seaborn>=0.12
tqdm>=4.60
